# Required for persistent storage
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sdg?schema=public"

# Repository driver mode: memory | prisma | auto
# Local development can use auto; production must use prisma.
GAME_SESSION_REPO="auto"
